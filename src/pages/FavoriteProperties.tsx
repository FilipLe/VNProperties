import React from "react"
import { View, Text } from "react-native"

export default function FavoriteProperties(): JSX.Element {
    return(
        <View>
            <Text>Favorite Properties</Text>
        </View>
    )
}